apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: systems-install
  namespace: flux-system
spec:
  interval: 10m0s
  path: kubernetes/apps/systems-install
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true