apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-noti
  namespace: flux-system
spec:
  interval: 10m0s
  path: kubernetes/apps/flux-noti
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  dependsOn:
    - name: systems
