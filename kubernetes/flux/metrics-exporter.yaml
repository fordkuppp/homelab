apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metrics-exporter
  namespace: flux-system
spec:
  interval: 10m0s
  path: kubernetes/apps/metrics-exporter
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  dependsOn:
    - name: systems
    - name: observability
    - name: postgres
