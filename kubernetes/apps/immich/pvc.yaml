apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-pvc
spec:
  accessModes: [ReadWriteMany]
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 100Gi
  volumeName: immich-pv