apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
  namespace: immich
spec:
  parentRefs:
    - name: tailscale-gateway
      namespace: tailscale
  hostnames:
    - "immich.local.fordkuppp.me"
  rules:
    - backendRefs:
        - name: immich-server
          port: 2283
