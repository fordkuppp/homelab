apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  parentRefs:
    - name: tailscale-gateway
      namespace: tailscale
  hostnames:
    - "jellyfin.local.fordkuppp.me"
  rules:
    - backendRefs:
        - name: jellyfin
          port: 8096
