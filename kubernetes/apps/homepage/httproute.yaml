apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
  namespace: homepage
spec:
  parentRefs:
    - name: tailscale-gateway
      namespace: tailscale
  hostnames:
    - "dash.local.fordkuppp.me"
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
