apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi-postgres
provisioner: nfs.csi.k8s.io
parameters:
  server: 192.168.1.11
  share: /mnt/general/data
  subDir: postgres
reclaimPolicy: Retain
volumeBindingMode: Immediate
mountOptions:
  - hard
  - nfsvers=3
