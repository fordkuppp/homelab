apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: uptime-kuma
  namespace: uptime-kuma
spec:
  parentRefs:
    - name: tailscale-gateway
      namespace: tailscale
  hostnames:
    - "uptime.local.fordkuppp.me"
  rules:
    - backendRefs:
        - name: uptime-kuma
          port: 3001
